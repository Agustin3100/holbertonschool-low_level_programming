/**
 * swap_int - print the alphabet
 * @a: int for characters
 * @b: int pointer
 *
 * Description: prints numberz
 * Return: value 0 (success)
 */
void swap_int(int *a, int *b)
{
int c;

c = *a;
*a = *b;
*b = c;
}
