/**
 * _isdigit - print the alphabet
 * @c: int for characters
 *
 * Description: prints numberz
 * Return: value 0 (success)
 */
int _isdigit(int c)
{
if (c >= '0' && c <= '9')
{    return (1);

}
else
{
return (0);
}
}
