#include <stdio.h>
#include <stdlib.h>
int *array_range(int min, int max)
{

        unsigned int i;
        char *p;

        p = malloc(sizeof(char)*size);

if (size == 0 || p == NULL)
{       return (NULL);
}
        for(i = 0; i < size; i++)
        {

                p[i] = c;
        }
 return(p);
}







}
