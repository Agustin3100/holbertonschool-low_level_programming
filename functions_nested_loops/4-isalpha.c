/**
 * _isalpha - print the alphabet
 * @c: int for characters
 *
 * Description: prints numberz
 * Return: value 0 (success)
 */

int _isalpha(int c)
{

if (c >= 'A' && c <= 'Z')
{
return (1);
}
else if (c >= 'a' && c <= 'z')

{
return (1);
}
else
{
return (0);
}
}
