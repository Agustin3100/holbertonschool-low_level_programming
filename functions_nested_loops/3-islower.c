/**
 * _islower - print the alphabet
 * @c: int for characters
 *
 * Description: prints numberz
 * Return: value 0 (success)
 */

int _islower(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (0);
}
else
{
return (1);
}
}
