/**
 * _abs - print the alphabet
 * @x: int for characters
 *
 * Description: prints numberz
 * Return: value 0 (success)
 */
#include "main.h"
int _abs(int x)
{
if (x > 0)
{  return (x);
}
else
{
return (x * -1);
}
}
